// Stubbed so api/client.ts can resolve Constants.expoConfig.hostUri without
// needing Expo's native config resolver at test time.

export default {
  expoConfig: {
    hostUri: 'localhost:8081',
  },
};
